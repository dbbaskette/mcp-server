# Default Configuration - SSE Transport
# Use spring.profiles.active to activate specific transport modes:
# - For SSE: spring.profiles.active=sse
# - For STDIO: spring.profiles.active=stdio

# Default to SSE profile if no profile is specified
spring.profiles.active=sse

# Base Configuration
spring.ai.mcp.server.name=mcp-server
spring.ai.mcp.server.version=1.0.0
